number_list = [1,2,3,4,5]
for num in number_list :

# adding items:
list = [1,2,3,4,5]
list.append(1)
list.insert(3,0) 
# parameters = index(where) and what to insert
list.remove(2)
# removes just the first occurence. remove by value 
del list[3]
# used to remove by the index
list.pop(index)
# remove from the list and return the value that was removed
sorted_list = sorted(list)
# doesnt change the original list
list.sort 
# change the original list

# sets
# set = set()

# intersection set1 & set
# union set1 | set2
# without intersection set1 ^ set2
# difference set1 - set2

